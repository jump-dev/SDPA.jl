# Documentation: http://docs.travis-ci.com/user/languages/julia/
dist: trusty
sudo: true
language: julia
os:
  - linux
  - osx
julia:
  - 0.7
  - 1.0
# G++ need to be updated to version 5 for C++14 to be supported
# https://docs.travis-ci.com/user/languages/cpp/
addons:
  apt_packages:
    - gfortran
    - liblapack-dev
    - libopenblas-dev
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5
matrix:
  allow_failures:
    - julia: nightly
notifications:
  email: false
# brew update for "Homebrew must be run under Ruby 2.3! You're running 2.0.0"
# brew cask uninstall oclint for https://github.com/travis-ci/travis-ci/issues/8826
before_install:
  - eval "CC=gcc-5 && CXX=g++-5" # see https://docs.travis-ci.com/user/languages/cpp/
  - sudo rm /usr/bin/gcc
  - sudo ln /usr/bin/gcc-5 /usr/bin/gcc
  - sudo ls -l /usr/bin/gcc
  - sudo rm /usr/bin/g++
  - sudo ln /usr/bin/g++-5 /usr/bin/g++
  - sudo ls -l /usr/bin/g++
  - if [ "$TRAVIS_OS_NAME" == "osx" ]; then brew update; brew cask uninstall oclint; brew install libtool gcc; fi
after_success:
  # push coverage results to Coveralls and Codecov
  - julia -e 'cd(Pkg.dir("SDPA")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder()); Codecov.submit(Codecov.process_folder())'
